#!/bin/sh

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# Entry point (sourced by .bashrc/.zshrc). Loads ordered fragments.
if [ -d ~/.commonrc.d ]; then
    for f in ~/.commonrc.d/*.sh; do
        [ -r "$f" ] && . "$f"
    done
fi
